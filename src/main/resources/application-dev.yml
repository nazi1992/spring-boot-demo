spring:
  datasource:
     driver-class-name: com.mysql.jdbc.Driver
     url: jdbc:mysql://localhost:3306/xf?useSSL=false
     username: root
     password: xiongfeng
  jpa:
      hibernate:
       ddl-auto: update
      show-sql: true
#ddl-auto  create 启动会清空且自动创建表，update 更新表

